{
  "name": "isocity-studio",
  "version": "0.1.0",
  "description": "Isometric city map editor — GIS layers, diamond grid zoning, game-ready export",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
